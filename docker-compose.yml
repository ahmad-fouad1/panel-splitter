services:
  panel-splitter:
    build: .
    container_name: panel-splitter-api-zip

    ports:
      - "8001:8001"

    volumes:
      - ./output:/app/output

    restart: unless-stopped

    networks:
      - evolution-network

networks:
  evolution-network:
    external: true